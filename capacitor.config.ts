import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app.relevamiento.visual.pps',
  appName: 'Cosa linda Cosa fea',
  webDir: 'www',
};

export default config;
